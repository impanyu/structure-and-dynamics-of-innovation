# C3 — 多博览专写: 10 T-shaped agents — read the WHOLE graph, write only inside
# their own semantic region (2 per subfield topic). Jumps allowed (whole graph,
# since read is unrestricted).
extends: ../stage1.yaml
run:
  run_id: core-broad
  seed: 0
  total_steps: 400
  generation_budget: 40
  agents:
    - {agent_id: b0, policy: llm, write_topics: ["large language models and natural language reasoning"], write_radius: 0.25}
    - {agent_id: b1, policy: llm, write_topics: ["large language models and natural language reasoning"], write_radius: 0.25}
    - {agent_id: b2, policy: llm, write_topics: ["computer vision and generative image models"], write_radius: 0.25}
    - {agent_id: b3, policy: llm, write_topics: ["computer vision and generative image models"], write_radius: 0.25}
    - {agent_id: b4, policy: llm, write_topics: ["reinforcement learning and sequential decision making"], write_radius: 0.25}
    - {agent_id: b5, policy: llm, write_topics: ["reinforcement learning and sequential decision making"], write_radius: 0.25}
    - {agent_id: b6, policy: llm, write_topics: ["graph neural networks and efficient deep learning architectures"], write_radius: 0.25}
    - {agent_id: b7, policy: llm, write_topics: ["graph neural networks and efficient deep learning architectures"], write_radius: 0.25}
    - {agent_id: b8, policy: llm, write_topics: ["adversarial robustness and trustworthy machine learning"], write_radius: 0.25}
    - {agent_id: b9, policy: llm, write_topics: ["adversarial robustness and trustworthy machine learning"], write_radius: 0.25}
